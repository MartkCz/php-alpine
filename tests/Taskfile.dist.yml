version: 3

tasks:
  run:
    desc: 'Run the tests'
    requires:
      vars: [IMAGE, PHP_VERSION]
    cmds:
      - sh '{{.TASKFILE_DIR}}/tests.sh' '{{.IMAGE}}:{{.PHP_VERSION}}{{.TAG}}'
  benchmark:
    desc: 'Run benchmark'
    requires:
      vars: [IMAGE, PHP_VERSION]
    cmds:
      - sh '{{.TASKFILE_DIR}}/benchmark.sh' '{{.IMAGE}}:{{.PHP_VERSION}}'
